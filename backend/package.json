{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "start:dev": "nodemon --exec 'json-server --watch db.json --port 3001'"
  },
  "keywords": [],
  "author": "Adrian Zawadzki",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
